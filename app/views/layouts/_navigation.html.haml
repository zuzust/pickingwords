= link_to "Pickingwords", root_path, class: 'brand'

- if user_signed_in?
  %ul.nav
    %li= link_to 'Tracked Words', tracked_words_path
    %li= link_to 'Picked Words', user_picked_words_path(current_user)

%ul.nav.pull-right
  %li= link_to 'Help', static_pages_help_path

  - if user_signed_in?
    %li= link_to 'Edit account', edit_user_registration_path
    %li= link_to 'Logout', destroy_user_session_path, method: 'delete'

  - else
    %li= link_to 'Login', new_user_session_path
